var _ = require('underscore');
var $ = require('jquery')


var app = app || {};

app = (function() {
    'use strict';

    var _cache = {
    },

    _init = function() {
    },

    _addEvents = function() {
    };

    return {
        init: _init
    };

})();

app.init();
